global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'backend'
    metrics_path: /metrics
    static_configs:
      - targets: ['backend:4000']

  # Optional: AI services scrape (requires a /metrics endpoint to be added)
  # - job_name: 'ai-services'
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ['ai:5001']

# TEST: Prometheus config valid
